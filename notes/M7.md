# M7 — Orchestrating Multiple Tools

## Plan from chat

Feature: Starred notes filter

Design from chat:

1. Data model changes
- Add `starred: false` field to each note dict
- Existing notes default to False if field missing

2. Backend changes
- Add toggle endpoint or action to flip `starred` boolean
- Add filtering logic for "starred only" view

3. UI changes
- Add star icon next to each note (click to toggle)
- Add filter option "Starred" in filter bar
- Highlight starred notes visually (optional styling)

4. Edge cases
- Existing notes without `starred` field must not break (treat as False)
- Multiple toggles should be idempotent
- Filtering should work when no notes are starred (empty list state)

5. Implementation plan (no code)
- Step 1: extend note model with `starred`
- Step 2: implement toggle logic
- Step 3: implement filtering logic
- Step 4: update UI rendering
- Step 5: test existing + new behavior

## Execute in Claude Code

Branch: m7-starred-notes
URL: https://github.com/sajnabhandari/applied-ai-sandbox/tree/m7-starred-notes

Acceptance criteria met:
- New notes created with `starred: False` by default
- POST /notes/<idx>/star toggles starred state
- GET /?filter=starred shows only starred notes; All shows everything

Claude flagged (not in the chat plan):
- The filter uses `n.get("starred", False)` instead of `n["starred"]` — necessary because any notes created before this feature would be missing the key entirely and would raise a KeyError without the `.get()` default

## Polish with Copilot

Two edits committed to m7-starred-notes:
- Renamed `show_starred` → `starred_only` in the `home` view for clarity (the old name read as a flag, the new one reads as a filter intent)
- Expanded the `toggle_star` docstring to explain the `.get()` defensive access and the silent out-of-range handling

## Chat review

One piece of feedback I acted on:
- **Bug: `loop.index0` is wrong in filtered view.** The template used `loop.index0` as the URL index for the toggle route, but in the starred-only filter that's the index within the filtered list — not the real position in `app.notes`. Toggling note 0 in a filtered view of 2 starred notes would hit `app.notes[0]`, which is the wrong note. Fixed by passing `(original_index, note)` tuples from the view so the form always submits the real index.

## Parallel work

Second task: note search feature (filter by keyword)
Worktree path: ../sandbox-task-b
Did I actually parallelize, or did one sit idle?
Mostly sequential — second task started after first finished, so limited parallel benefit.

## Retro

I used chat for planning the starred notes feature, which helped clarify the data model and UI structure before any coding. Claude Code handled implementation, especially adding the starred field and filter logic. Copilot was useful only for small edits like renaming variables and adding docstrings, where agent-level overhead would be unnecessary.

The chat review caught a missing edge case: handling notes without a starred field safely in filtering logic. I fixed this by defaulting missing values to False.

If I redid this, I would tighten the initial chat design to explicitly define filter behavior before implementation to avoid minor ambiguity during coding.

Most of my time as conductor was spent during review and debugging rather than planning, which is correct because that's where correctness risk is highest.
