# M5 — Task Decomposition

## Decomposition (8-10 tasks)

1. Add `tags` key to note dict (default empty list). Verify: new notes have `tags == []`.
2. Add tag input field to note creation form. Verify: form accepts comma-separated tags.
3. Parse comma-separated tag string into list. Verify: "work, urgent" → ["work", "urgent"].
4. Store parsed tags in note object. Verify: created note includes correct tags.
5. Show tags in notes list UI as chips. Verify: tags visible next to each note.
6. Add filter bar UI ("All / tag values"). Verify: filter options render.
7. Implement filtering logic by tag. Verify: selecting a tag filters notes correctly.
8. Add tests for tag parsing. Verify: tests pass.
9. Add tests for filtering behavior. Verify: tests pass and existing tests still pass.

## Task 1 context brief

Goal:
Add a `tags` field to the note structure.

Constraints:
- Do not change existing fields (title, body)
- Tags must default to an empty list
- No database changes (in-memory dict only)
- No UI changes yet

Files I expect to change:
- app.py

Acceptance:
- New notes include "tags": []
- Existing tests still pass

## Task 1 — what happened

Claude's plan summary:
Added a tags field to the note dict with default empty list.

What I pushed back on:
Nothing.

Verification result:
New notes include "tags": []. Existing tests still pass.

## Task 2 — what happened

Claude's plan summary:
Added parsing for comma-separated tags and stored result in note creation.

What I pushed back on:
Nothing.

Verification result:
"work, urgent" correctly becomes ["work", "urgent"]. Tests pass.

## Task 3 — what happened
- ...

## Reflection — where I redirected
- (3 sentences)
