# M5 — Task Decomposition

## Decomposition (8-10 tasks)

1. Extend note structure to include `tags` list (default empty). Verify: existing tests still pass; new note has `tags == []`.

2. Update note creation logic to accept tags input. Verify: creating note with "work, urgent" produces `["work", "urgent"]`.

3. Add helper function to parse comma-separated tags. Verify: unit test for parsing works.

4. Update notes storage/display logic to carry tags through. Verify: notes still render without errors.

5. Update UI to show tags as chips next to note title. Verify: tags visible in UI for created note.

6. Add filter bar ("All / tag1 / tag2"). Verify: UI renders filter options.

7. Implement filtering logic for tags in backend or frontend. Verify: selecting tag filters notes correctly.

8. Add tests for tag parsing + filtering behavior. Verify: pytest passes.

## Task 1 context brief

Goal:
Add a `tags` field to the note structure.

Constraints:
- Do not change existing note fields (title, body).
- Tags must default to an empty list.
- No database changes (in-memory dict only).

Files I expect to change:
- app.py (or wherever notes are created)

Acceptance:
- New notes include `"tags": []`
- Existing tests still pass
- No UI changes yet

## Task 1 — what happened
- Claude's plan summary:
- What I pushed back on:
- Verification result:

## Task 2 — what happened
- ...

## Task 3 — what happened
- ...

## Reflection — where I redirected
- (3 sentences)
