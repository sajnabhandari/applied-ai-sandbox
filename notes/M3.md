# M3 — Context Engineering

_Cohort notes for the M3 module. Edited as I work through it._

## Step 2 — CLAUDE.md behaviors

- It tells Claude not to edit test files to make them pass — without this, Claude will absolutely rewrite the spec to fit its own broken implementation.
- It names the entry point (`app.py`) and how to run it (`python app.py`) — without this, Claude wastes tokens grepping around before doing any real work.
- It says run `pytest` after each substantive change — without this, Claude ships code it hasn't verified, treating type-checking as a substitute for actually running the tests.
